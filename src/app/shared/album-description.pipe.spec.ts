import { AlbumDescriptionPipe } from './album-description.pipe';

describe('AlbumDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
